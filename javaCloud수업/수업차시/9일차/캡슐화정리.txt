	s.append("캡슐화  (encapsulation)") ;		 
		s.append("   : 데이터와 데이터를 다루는 매서드(기능)가 하나로 묶여있음 ") ;
                s.append("   : 생성된 객체를 통해서 매서드를 호출할 수 있다 ") ;
		s.append("    생성된 객체를 통해서 매서드를 호출할 때 객체의 정보가 전달된다
                                   매서드에서는 매개변수로 전달받는다 ( 이때 매개변수의 이름이  this  ,  자기참조형변수  라고 한다)
		s.append("    생성자, 접근제어자를 통해서 단단한 캡슐을 보장함");
		s.append("   데이터보호를 얻음 ( 특정변수(객체)의 값을 정해진 방법으로만 다룰 수 있게 하겠다) ");
		




   데이터 + 함수  왜 묶나 ?
   데이터보호는  어떻게 이루어지는가?



  => 서로 관련이 있기 때문에 데이터가 변하면 함수가 바뀌어야 한다 
       같이 있으면 수정하기가 쉬워진다


  => 접근제한자를 두어 
      정해진 매서드를 통해서만 변경하게 한다 

   

		
		
